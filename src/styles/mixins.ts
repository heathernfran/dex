import { css } from 'styled-components'

const centeredFlex = css`
  align-items: center;
  display: flex;
  justify-content: center;
`

const scrollable = css`
  height: 100%;
  overflow-y: auto;
`

export { centeredFlex, scrollable }
